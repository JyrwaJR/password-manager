import 'package:flutter/material.dart';

class Volt extends StatelessWidget {
  const Volt({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text("Volt"),
    );
  }
}
